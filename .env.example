# Frontend
VITE_API_URL=http://localhost:3001

# Set to true (or use `npm run dev:mock`) to run without Docker/API/DB.
# VITE_MOCK=true

# See also server/.env.example for DATABASE_URL, JWT_SECRET, Google/Apple OAuth.
